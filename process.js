// This file will process the data we grab from the webpage for a given department
function processQueryArray (professor_name) {

    // Fill in with algorithm logic later
    console.log("Process: " + professor_name);
    return professor_name; 
}

module.exports.processQueryArray = processQueryArray;